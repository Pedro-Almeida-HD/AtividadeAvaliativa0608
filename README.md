# AtividadeAvaliativa0608
 4. A lista ordenada utiliza números e números romanos por exemplo, já a não ordenada não vai utilizar a numeração dos elementos
5.  O display no CSS serve para escolher as opções de movimentação na Div, o inline coloca uma configuração dentro de apenas um elemento

6. renderiza cada elemento de acordo com a definição do CSS como margem ,borda e preenchimento; O posicionamento coloca um texto por exemplo mais para esquerda ou direita, A margem vai colocar um espaço entre o texto e os  borda da tela e o Preenchimento é as configurações do espaçamento dentro do BOX

 7. São tags que organizam a pagina o header ele separa o cabeçalho do resto da pagina; O section separa os elementos  na página; O footer é usado para a criação de rodapés nas paginas de empresas.

8. 

9. O flex é a propriedade que move os elementos  dentro  do código,por exemplo mover uma imagem e centraliza-la

15. A const  é um valor constante;A let apenas para um único escopo.

17.
